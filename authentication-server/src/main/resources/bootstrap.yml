server:
  port: 8081
 
spring:
  application:
    name: authentication-server
  profiles:
    active: dev

jwt:
  signing_key: fuhongchao
  keypair:
    file: jwt.jks
    key: jwt
    pwd: fhc0618

security:
  oauth2:
    client:
      client-id: client_fhc
      client-secret: fhc0618
      scope: all
    resource:
      id: authentication

# 端点监控配置
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always